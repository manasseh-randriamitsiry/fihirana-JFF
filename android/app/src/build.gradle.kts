// Duplicate build.gradle.kts stub placed under android/app/src. This file is intentionally left blank
// to avoid conflicting with the main `android/app/build.gradle.kts`.
// If you need to restore it, re-add the correct configuration in the main build file instead.
